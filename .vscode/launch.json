{
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/src/main",
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [
                {
                    "name": "SCRIPT",
                    "value": "license_plate_detector"
                }
            ],
            "externalConsole": false,
            "MIMode": "lldb",
            
        }
    ],
    "version": "2.0.0"
}